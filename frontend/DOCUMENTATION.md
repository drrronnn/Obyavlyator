# Документация проекта "Объявлятор" frontend

## 📋 Общая информация о проекте

### Назначение
**Объявлятор** — веб-приложение для управления объявлениями о недвижимости. Система предназначена для риелторов и агентств недвижимости, позволяя:
- Просматривать объявления из различных источников (Cian, Avito)
- Управлять избранными объявлениями
- Отслеживать статистику работы сотрудников
- Получать уведомления о новых объявлениях в реальном времени

### Целевая аудитория
- Риелторы
- Агентства недвижимости
- Менеджеры по работе с недвижимостью

### Основные технологии

| Технология | Версия | Назначение |
|-----------|--------|-----------|
| **React** | 18.3.1 | Основной UI-фреймворк |
| **TypeScript** | 5.6.3 | Типизация кода |
| **Vite** | 6.0.11 | Сборщик и dev-сервер |
| **HeroUI** | 2.x | UI-компоненты |
| **Tailwind CSS** | 4.1.11 | Стилизация |
| **Zustand** | 5.0.7 | Управление состоянием |
| **React Query** | 5.89.0 | Управление серверным состоянием |
| **React Router** | 6.23.0 | Маршрутизация |
| **Axios** | 1.12.2 | HTTP-клиент |
| **Socket.io** | 4.8.1 | WebSocket соединения |
| **React Hook Form** | 7.62.0 | Управление формами |
| **Yup** | 1.7.0 | Валидация форм |
| **Framer Motion** | 11.18.2 | Анимации |

---

## 🏗️ Структура проекта

```
obyavltayor/
├── .amazonq/              # Правила для Amazon Q AI
│   └── rules/
├── public/                # Статические файлы
├── src/
│   ├── assets/           # Ресурсы (иконки, изображения)
│   │   ├── icons/        # SVG иконки
│   │   └── images/       # Логотипы
│   ├── components/       # React компоненты
│   │   ├── modals/       # Модальные окна
│   │   └── ui/           # UI компоненты
│   ├── config/           # Конфигурация приложения
│   ├── layouts/          # Layout компоненты
│   ├── pages/            # Страницы приложения
│   ├── shared/           # Общие модули
│   │   ├── constants/    # Константы
│   │   ├── guard/        # Route guards
│   │   ├── hooks/        # Custom hooks
│   │   ├── services/     # API сервисы
│   │   └── utils/        # Утилиты
│   ├── store/            # Zustand stores
│   ├── styles/           # Глобальные стили
│   ├── types/            # TypeScript типы
│   ├── App.tsx           # Главный компонент
│   ├── main.tsx          # Точка входа
│   └── provider.tsx      # Провайдеры
├── package.json
├── vite.config.ts
└── tsconfig.json
```

---

## 🔄 Техническая архитектура

### Поток данных

```
API (Backend) 
    ↓
apiClient (Axios) → authService/listingService/profileService
    ↓
React Query / Zustand Store
    ↓
Custom Hooks (useAuth, useListings, useWebSocket)
    ↓
React Components (Pages/UI)
    ↓
User Interface
```

### WebSocket поток

```
WebSocket Server (ws://localhost:8001/ws)
    ↓
socketService
    ↓
useWebSocket hook
    ↓
React Query Cache Update
    ↓
UI Auto-Update
```

---

## 📦 Основные модули

### 1. **Authentication (Аутентификация)**

**Расположение:** `src/shared/services/authService.ts`, `src/store/authStore.ts`

**Функциональность:**
- Регистрация пользователей
- Вход через email/пароль
- Вход через Google OAuth
- Восстановление пароля
- Верификация email через OTP

**Ключевые функции:**
- `registerUser()` - регистрация
- `loginUser()` - вход
- `verifyEmail()` - подтверждение email
- `forgotPassword()` - восстановление пароля
- `resetPassword()` - сброс пароля

**Store:** `useAuthStore`
- Хранит: `user`, `token`, `isAuthenticated`
- Персистентность через `localStorage`

**Используется в:**
- `LoginPage.tsx`
- `RegisterPage.tsx`
- `ConfirmPasswordPage.tsx`
- `ProtectedRoute.tsx`

---

### 2. **Listings (Объявления)**

**Расположение:** `src/shared/services/listingService.ts`

**Функциональность:**
- Получение списка объявлений с пагинацией
- Фильтрация по параметрам (цена, площадь, комнаты, источник)
- Поиск по адресу
- Трансформация данных из API в UI-формат

**Ключевые функции:**
- `listingApi.getListings()` - получение объявлений
- `transformFiltersToApi()` - преобразование фильтров
- `transformListingToAd()` - преобразование данных
- `useListings()` - React Query hook

**Типы данных:**
```typescript
interface Listing {
  id: string;
  deal_type: string;
  price: number;
  total_meters: number;
  floor: string;
  location: string;
  source: string;
  url: string;
  phone_number: string;
  rooms_count: number | null;
  is_favorite: boolean;
}
```

**Используется в:**
- `AdsPage.tsx`
- `RentPage.tsx`
- `FavoritesPage.tsx`

---

### 3. **WebSocket (Реальное время)**

**Расположение:** `src/shared/services/socketService.ts`, `src/shared/hooks/useWebSocket.ts`

**Функциональность:**
- Подключение к WebSocket серверу
- Получение новых объявлений в реальном времени
- Автоматическое обновление кэша React Query
- Ping/pong для поддержания соединения

**Ключевые компоненты:**
- `socketService` - singleton для управления WebSocket
- `useWebSocket()` - hook для интеграции с React
- `useFavoritesWebSocket()` - отслеживание избранного

**Используется в:**
- `App.tsx` (глобально)

---

### 4. **Profile (Профиль пользователя)**

**Расположение:** `src/shared/services/profileService.ts`, `src/store/authStore.ts`

**Функциональность:**
- Просмотр профиля
- Редактирование имени/фамилии
- Смена email (с OTP подтверждением)
- Смена пароля

**Ключевые функции:**
- `profileApi.getProfile()`
- `profileApi.updateProfile()`
- `profileApi.changeEmail()`
- `profileApi.confirmEmailChange()`
- `profileApi.changePassword()`

**Используется в:**
- `ProfilePage.tsx`
- Модальные окна профиля

---

### 5. **Favorites (Избранное)**

**Расположение:** `src/store/favoritesStore.ts`, `src/shared/services/favoritesService.ts`

**Функциональность:**
- Добавление/удаление из избранного
- Синхронизация через WebSocket
- Локальное кэширование состояния

**Store:** `useFavoritesStore`
- `favoriteIds: Set<string>` - ID избранных объявлений
- `addFavorite()` / `removeFavorite()` - управление

**Используется в:**
- `AdsTable.tsx`
- `FavoritesPage.tsx`

---

### 6. **Filters (Фильтрация)**

**Расположение:** `src/store/adsFilterStore.ts`, `src/store/rentFilterStore.ts`, `src/store/favoritesFilterStore.ts`

**Функциональность:**
- Хранение состояния фильтров для каждой страницы
- Фильтрация по: типу сделки, источнику, количеству комнат, цене, площади
- Поиск по адресу

**Stores:**
- `useAdsFilterStore` - для страницы объявлений
- `useRentFilterStore` - для страницы аренды
- `useFavoritesFilterStore` - для избранного

**Используется в:**
- `FilterForm.tsx`
- `AdsPage.tsx`
- `RentPage.tsx`
- `FavoritesPage.tsx`

---

## 🎨 UI Компоненты

### Layouts

#### `DashboardLayout.tsx`
**Назначение:** Основной layout для авторизованных пользователей

**Структура:**
- Header с логотипом, информацией о подписке, меню пользователя
- Sidebar (desktop) / Drawer (mobile)
- Основной контент
- Footer

**Используется в:** Все защищенные страницы

#### `AuthLayout.tsx`
**Назначение:** Layout для страниц авторизации

**Используется в:** Login, Register, ConfirmPassword

---

### UI Components (`src/components/ui/`)

#### `AdsTable.tsx`
**Назначение:** Таблица объявлений с пагинацией

**Функции:**
- Отображение списка объявлений
- Пагинация
- Действия: добавить в избранное, редактировать, удалить
- Индикация новых объявлений

**Props:**
```typescript
{
  ads: Ad[];
  total: number;
  page: number;
  totalPages: number;
  onPageChange: (page: number) => void;
}
```

#### `FilterForm.tsx`
**Назначение:** Форма фильтрации объявлений

**Поля:**
- Тип сделки (продажа/аренда)
- Источник (Cian/Avito)
- Количество комнат
- Диапазон цены
- Диапазон площади
- Поиск по адресу

#### `Sidebar.tsx`
**Назначение:** Боковое меню навигации

**Элементы:**
- Объявления
- Сдается
- Избранное (с счетчиком новых)
- Сотрудники

#### `EmployeeCard.tsx` / `EmployeeStatsCard.tsx`
**Назначение:** Карточки сотрудников и их статистики

**Используется в:** `EmployeesPage.tsx`

---

### Modals (`src/components/modals/`)

Используется библиотека `@ebay/nice-modal-react` для управления модальными окнами.

**Регистрация модалок:** `src/components/modals/index.ts`

**Типы модалок:**
- `ad/` - модалки для работы с объявлениями
- `employees/` - модалки для управления сотрудниками
- `profile/` - модалки профиля
- `rent/` - модалки для аренды
- `LogoutConfirmModal.tsx` - подтверждение выхода

**Использование:**
```typescript
import NiceModal from '@ebay/nice-modal-react';

NiceModal.show('modal-id', { props });
```

---

## 🔐 Guards (Защита маршрутов)

### `ProtectedRoute.tsx`
**Назначение:** Защита маршрутов от неавторизованных пользователей

**Логика:**
- Проверяет `isAuthenticated` из `useAuthStore`
- Если не авторизован → редирект на `/auth/login`
- Сохраняет текущий путь для возврата после авторизации

### `AuthRoute.tsx`
**Назначение:** Защита страниц авторизации от авторизованных пользователей

**Логика:**
- Если авторизован → редирект на главную страницу

---

## 🪝 Custom Hooks

### `useAuth.ts`
**Назначение:** Хуки для работы с аутентификацией

**Экспортируемые хуки:**
- `useRegisterMutation()` - регистрация
- `useVerifyEmailMutation()` - верификация email
- `useLoginMutation()` - вход
- `useForgotPasswordMutation()` - восстановление пароля
- `useResetPasswordMutation()` - сброс пароля

**Особенности:**
- Используют React Query mutations
- Автоматическая обработка ошибок через toast
- Обновление store после успешных операций

### `useWebSocket.ts`
**Назначение:** Управление WebSocket соединением

**Функциональность:**
- Автоматическое подключение при монтировании
- Обработка новых объявлений
- Обновление React Query кэша
- Отключение при размонтировании

### `useFavoritesWebSocket.ts`
**Назначение:** Отслеживание избранного через WebSocket

### `useFilter.ts`
**Назначение:** Управление состоянием фильтров

### `useDebounce.ts`
**Назначение:** Debounce для поиска

### `useMediaQuery.ts`
**Назначение:** Определение размера экрана

### `useCachedValue.ts`
**Назначение:** Кэширование значений

---

## 🗄️ State Management (Zustand)

### `authStore.ts`
**Состояние:**
```typescript
{
  user: User | null;
  token: string | null;
  isAuthenticated: boolean;
  isProfileLoading: boolean;
}
```

**Действия:**
- `setUser()`, `setToken()`
- `loginWithGoogle()`
- `logout()`
- `fetchProfile()`, `updateProfile()`
- `changeEmail()`, `confirmChangeEmail()`, `changePassword()`

**Персистентность:** localStorage (`auth-storage`)

### `generalStore.ts`
**Состояние:**
```typescript
{
  isSidebarOpen: boolean;
}
```

**Действия:**
- `setIsSidebarOpen()`, `toggleSidebar()`

### `favoritesStore.ts`
**Состояние:**
```typescript
{
  favoriteIds: Set<string>;
  isConnected: boolean;
}
```

**Действия:**
- `addFavorite()`, `removeFavorite()`
- `setFavorites()`, `isFavorite()`

### Filter Stores
- `adsFilterStore.ts`
- `rentFilterStore.ts`
- `favoritesFilterStore.ts`

**Общая структура:**
```typescript
{
  dealType: string;
  source: string;
  rooms: string[];
  priceRange: [number, number];
  areaRange: [number, number];
  search: string;
  page: number;
}
```

---

## 🌐 API Integration

### `apiClient.ts`
**Назначение:** Настроенный Axios instance

**Конфигурация:**
- Base URL: `http://localhost:8001`
- Автоматическое добавление JWT токена в headers
- Обработка ошибок API

**Interceptors:**
- Request: добавление `Authorization: Bearer <token>`
- Response: обработка ошибок с кодами

### Сервисы

#### `authService.ts`
**Endpoints:**
- `POST /auth/register` - регистрация
- `POST /auth/verify-email` - верификация
- `POST /auth/login` - вход
- `GET /auth/me` - текущий пользователь
- `POST /auth/forgot-password` - восстановление
- `POST /auth/reset-password` - сброс пароля

#### `listingService.ts`
**Endpoints:**
- `GET /listings?page=1&...filters` - список объявлений

**Query Keys:**
```typescript
listingQueryKeys.filtered(page, filters)
```

#### `profileService.ts`
**Endpoints:**
- `GET /profile` - получить профиль
- `PUT /profile` - обновить профиль
- `POST /profile/change-email` - смена email
- `POST /profile/confirm-email-change` - подтверждение email
- `POST /profile/change-password` - смена пароля

#### `favoritesService.ts`
**Endpoints:**
- `POST /favorites` - добавить в избранное
- `DELETE /favorites/{id}` - удалить из избранного

---

## 📄 Страницы

### Публичные страницы

#### `LoginPage.tsx`
**Путь:** `/auth/login`

**Функциональность:**
- Вход через email/пароль
- Вход через Google OAuth
- Ссылка на регистрацию
- Ссылка на восстановление пароля

#### `RegisterPage.tsx`
**Путь:** `/auth/register`

**Функциональность:**
- Регистрация нового пользователя
- Валидация формы (Yup)
- Переход на страницу подтверждения

#### `ConfirmPasswordPage.tsx`
**Путь:** `/auth/confirm-password`, `/forgot-password`

**Функциональность:**
- Ввод OTP кода
- Верификация email
- Сброс пароля

### Защищенные страницы

#### `AdsPage.tsx`
**Путь:** `/` (главная)

**Функциональность:**
- Список всех объявлений
- Фильтрация
- Пагинация
- Добавление в избранное
- Реал-тайм обновления

#### `RentPage.tsx`
**Путь:** `/rent`

**Функциональность:**
- Объявления только по аренде
- Фильтрация
- Перемещение в основные объявления

#### `FavoritesPage.tsx`
**Путь:** `/favorites`

**Функциональность:**
- Избранные объявления
- Счетчик новых
- Удаление из избранного

#### `EmployeesPage.tsx`
**Путь:** `/employees`

**Функциональность:**
- Список сотрудников
- Статистика по каждому
- Добавление/редактирование/удаление

#### `ProfilePage.tsx`
**Путь:** `/profile`

**Функциональность:**
- Просмотр профиля
- Редактирование данных
- Смена email
- Смена пароля

#### `NotFoundPage.tsx`
**Путь:** `*` (404)

---

## 🔧 Конфигурация

### `constants.ts`
**Содержит:**
- `MENU_ITEMS` - элементы меню
- `APP_CONFIG` - конфигурация приложения
- `ICONS` - импорты иконок
- `MOCK_EMPLOYEES` - моковые данные сотрудников

### `validationSchemas.ts`
**Содержит:** Yup схемы валидации для форм

### `queryClient.ts`
**Содержит:** Конфигурация React Query

### `googleProvider.tsx`
**Содержит:** Обертка для Google OAuth

---

## 🚀 Запуск проекта

### Установка зависимостей
```bash
npm install
```

### Запуск dev-сервера
```bash
npm run dev
```

### Сборка для production
```bash
npm run build
```

### Предпросмотр production сборки
```bash
npm run preview
```

### Линтинг
```bash
npm run lint
```

---

## 🔄 Основные сценарии работы

### 1. Авторизация пользователя

```
Пользователь → LoginPage
    ↓
useLoginMutation() → authService.loginUser()
    ↓
API возвращает access_token
    ↓
authStore.setToken() → localStorage
    ↓
authService.getCurrentUser()
    ↓
authStore.setUser()
    ↓
ProtectedRoute разрешает доступ
    ↓
Редирект на главную страницу
```

### 2. Загрузка объявлений

```
AdsPage монтируется
    ↓
useListings(page, filters) → React Query
    ↓
listingApi.getListings() → API
    ↓
Данные кэшируются в React Query
    ↓
transformListingToAd() → UI формат
    ↓
AdsTable отображает данные
```

### 3. Реал-тайм обновления

```
App.tsx → useWebSocket()
    ↓
socketService.connect() → WebSocket
    ↓
Сервер отправляет новые объявления
    ↓
socketService.onNewListings()
    ↓
queryClient.setQueryData() → обновление кэша
    ↓
AdsTable автоматически перерисовывается
```

### 4. Добавление в избранное

```
Пользователь кликает на ❤️
    ↓
favoritesService.addToFavorite()
    ↓
API добавляет в избранное
    ↓
WebSocket отправляет обновление
    ↓
favoritesStore.addFavorite()
    ↓
UI обновляется
```

---

## 🎯 Рекомендации по расширению

### Добавление нового API endpoint

1. **Создать функцию в сервисе:**
```typescript
// src/shared/services/myService.ts
export const myApi = {
  getData: async () => {
    const response = await apiClient.get('/my-endpoint');
    return response.data;
  }
};
```

2. **Создать React Query hook:**
```typescript
export const useMyData = () => {
  return useQuery({
    queryKey: ['my-data'],
    queryFn: myApi.getData,
  });
};
```

3. **Использовать в компоненте:**
```typescript
const { data, isLoading } = useMyData();
```

### Добавление новой страницы

1. **Создать компонент страницы:**
```typescript
// src/pages/MyPage.tsx
const MyPage = () => {
  return (
    <DashboardLayout>
      <PageHeader title="Моя страница" />
      {/* контент */}
    </DashboardLayout>
  );
};
```

2. **Добавить маршрут в App.tsx:**
```typescript
<Route
  path="/my-page"
  element={
    <ProtectedRoute>
      <MyPage />
    </ProtectedRoute>
  }
/>
```

3. **Добавить в меню (constants.ts):**
```typescript
{
  key: "my-page",
  label: "Моя страница",
  icon: MyIcon,
  href: "/my-page",
  newItems: 0,
}
```

### Добавление нового store

1. **Создать store:**
```typescript
// src/store/myStore.ts
import { create } from 'zustand';

interface MyStore {
  data: any;
  setData: (data: any) => void;
}

export const useMyStore = create<MyStore>((set) => ({
  data: null,
  setData: (data) => set({ data }),
}));
```

2. **Использовать в компоненте:**
```typescript
const { data, setData } = useMyStore();
```

### Добавление нового UI компонента

1. **Создать компонент:**
```typescript
// src/components/ui/MyComponent.tsx
interface MyComponentProps {
  title: string;
}

export const MyComponent: React.FC<MyComponentProps> = ({ title }) => {
  return <div>{title}</div>;
};
```

2. **Экспортировать из index.ts:**
```typescript
// src/components/index.ts
export { MyComponent } from './ui/MyComponent';
```

---

## ⚠️ Важные замечания

### Проблемные места

1. **Моковые данные сотрудников** (`constants.ts`)
   - Используются захардкоженные данные
   - Рекомендация: создать API endpoint для сотрудников

2. **Закомментированный DashboardPage**
   - В `App.tsx` есть закомментированный маршрут
   - Рекомендация: удалить или реализовать

3. **Hardcoded API URL**
   - В `apiClient.ts` и `socketService.ts` захардкожен `localhost:8001`
   - Рекомендация: вынести в `.env` файл

4. **Отсутствие обработки ошибок WebSocket**
   - В `socketService.ts` нет обработки `onerror` и `onclose`
   - Рекомендация: добавить переподключение

### Улучшения производительности

1. **Lazy loading страниц:**
```typescript
const AdsPage = lazy(() => import('./pages/AdsPage'));
```

2. **Мемоизация компонентов:**
```typescript
export const MyComponent = memo(({ data }) => {
  // ...
});
```

3. **Виртуализация списков:**
- Для больших списков объявлений использовать `react-window`

### Безопасность

1. **Токен в localStorage**
   - Уязвимость к XSS атакам
   - Рекомендация: рассмотреть httpOnly cookies

2. **Валидация на клиенте**
   - Всегда дублировать валидацию на сервере

---

## 📚 Полезные ссылки

- [React Documentation](https://react.dev)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/)
- [Zustand Documentation](https://zustand-demo.pmnd.rs/)
- [React Query Documentation](https://tanstack.com/query/latest)
- [HeroUI Documentation](https://heroui.com)
- [Tailwind CSS Documentation](https://tailwindcss.com)

---

## 📝 Changelog

### Version 0.0.0 (Текущая)
- Базовая функциональность авторизации
- Просмотр объявлений с фильтрацией
- Избранное
- Управление сотрудниками
- WebSocket для реал-тайм обновлений
- Профиль пользователя

---

**Документация создана:** 2025
**Последнее обновление:** 2025
